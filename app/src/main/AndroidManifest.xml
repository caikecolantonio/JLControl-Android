<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jlima">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:name=".JLimaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/TemaJlima"
        android:usesCleartextTraffic="true">
        <activity android:name=".ListaTernoCadastroActivity"></activity>
        <activity
            android:name=".ConsultarActivity"
            android:theme="@style/AppTheme.Toolbar" />
        <activity
            android:name=".TernoActivity"
            android:theme="@style/AppTheme.Toolbar" />
        <activity
            android:name=".AcaoActivity"
            android:theme="@style/AppTheme.Toolbar" />
        <activity
            android:name=".LocarActivity"
            android:theme="@style/AppTheme.Toolbar" />
        <activity
            android:name=".InicialActivity"
            android:parentActivityName=".TernoActivity"
            android:theme="@style/AppTheme.Toolbar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name=".MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>